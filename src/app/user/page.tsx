import { AccountView } from "src/sections/account/view";

export const metadata = {
  title: "Hulya Users: Profile Settings",
};

export default function UserProfilePage() {
  return <AccountView />;
}
