// sections
import { ArtistCreateView } from 'src/sections/artists/view';

// ----------------------------------------------------------------------

export const metadata = {
  title: 'Dashboard: Create a new artist',
};

export default function ArtistCreatePage() {
  return <ArtistCreateView />;
}
