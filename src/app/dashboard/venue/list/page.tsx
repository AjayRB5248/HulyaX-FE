// sections
import { VenueListView } from 'src/sections/venues/view';

// ----------------------------------------------------------------------

export const metadata = {
  title: 'Dashboard: Venue List',
};

export default function VenueListPage() {
  return <VenueListView />;
}
