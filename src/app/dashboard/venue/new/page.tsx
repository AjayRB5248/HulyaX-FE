// sections
import { VenueCreateView } from 'src/sections/venues/view';

// ----------------------------------------------------------------------

export const metadata = {
  title: 'Dashboard: Create a new venue',
};

export default function VenueCreatePage() {
  return <VenueCreateView />;
}
