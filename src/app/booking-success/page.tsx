import { BookingSuccess } from "src/sections/success";

export const metadata = {
  title: "Ticket Booking Success.",
};

export default function BookingSuccessPage() {
  return <BookingSuccess />;
}
