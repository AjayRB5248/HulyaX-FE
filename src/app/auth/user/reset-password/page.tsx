// sections
import { UserResetPassword } from "src/sections/auth/user";

// ----------------------------------------------------------------------

export const metadata = {
  title: "Forgot Password?",
};

export default function LoginPage() {
  return <UserResetPassword />;
}
